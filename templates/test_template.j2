from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

{% for test in tests %}
def test_{{ test.operationId }}():
    response = client.{{ test.method.lower() }}("{{ test.path }}")
    assert response.status_code in (200, 201, 404)
{% endfor %}
